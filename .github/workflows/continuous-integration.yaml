name: API Tests with Cypress
on: 
  push:
    branches: [ master ]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:        
      - name: Checkout
        uses: actions/checkout@v3
      - name: Update browserList
        uses: npx browserslist@latest --update-db 
      - name: Cypress run 🧪
        uses: cypress-io/github-action@v2